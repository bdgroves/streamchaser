[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "streamchaser"
version = "1.0.0"
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["src/streamchaser"]
